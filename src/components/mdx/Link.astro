---
export interface Props {
  href: string;
  target?: HTMLAnchorElement["target"];
  style?: string;
}

const props = Astro.props;
---

<a class="text-blue-600 hover:underline" href={props.href} target={props?.target} style={props.style}>
  <slot />
</a>
